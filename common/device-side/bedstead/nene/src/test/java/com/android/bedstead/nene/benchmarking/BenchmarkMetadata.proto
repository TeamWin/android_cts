syntax = "proto3";

package com.android.bedstead.nene.benchmarking;

option java_multiple_files = true;

// Describes all relevant info about a benchmark.
message BenchmarkMetadata {
  // The unique name of the benchmark.
  string name = 1;
}
